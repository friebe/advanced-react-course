#public facing api with we can interact with
# example
# type Dog {
#   name: String!
# }

# type User {
#   id: ID!
#   name: String!
# }

# type Mutation {
#   createDog(name: String!): Dog
# }

# type Query {
#   dogs: [Dog]!
# }

# import * from "./generated/prisma.graphql"

type Mutation {
  createItem(
    title: String
    description: String
    price: Int
    image: String
    largeImage: String
  ): Item!
}

type Query {
  items: [Item]!
}
